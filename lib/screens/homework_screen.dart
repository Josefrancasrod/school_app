import 'package:flutter/material.dart';

class HomeworkScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Center(
          child: Text('My Homeworks'),
        ),
      ),
      body: Center(
        child: Text('Homework screen'),
      ),
    );
  }
}
